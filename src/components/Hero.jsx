function Hero() {
  return (
    <div className="hero">
      <h1>CAPTURE EVERYTHING</h1>
      <p>Express yourself without limitation.</p>
      <div>
        <a className="link">JOIN THE BETA →</a>
      </div>
    </div>
  );
}

export default Hero;
